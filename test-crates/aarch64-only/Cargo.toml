[package]
name = "test-aarch64-only"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
multiversed = { path = "../..", default-features = false, features = [
    "arm64",
] }
multiversion = "0.8"
